/**
 * Módulo virtual generado por `vite-plugin-building-images`.
 * Mapa: nombre de carpeta en `public/edificios/` -> URLs públicas de sus imágenes.
 */
declare module 'virtual:building-images' {
  const map: Record<string, string[]>;
  export default map;
}
